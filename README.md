# WLib

Patched WindUI library for Abyss Fisher.

## Changes from official WindUI

- Fixed `ConfigManager:Init()` — uses correct file paths for loading existing configs
- Fixed AutoLoad — properly merges UI elements before loading, increased delay for HTTP-loaded tabs
